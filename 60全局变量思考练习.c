/*全局变量思考练习*/
#include <stdio.h>

int a=5;
int add();

int main(){
    
    /*全局变量与局部变量区别是什么?
     
    1.生命生期不一样;全局变量跟随程序,而局部变量跟随作用域.
    2.全局变量可以被其它函数共享(前题:在变量之下定义),
      而局部变量只能在本函数数内用.
    3.全局变量在静态区,而局部变量在栈区.
     
     */
    
    add();
    return 0;
}

/*实现一个函数调用本身不会出现死递归的案例.*/
int add(){
    
    //int a=0;//这样定义就成死递归了
    a++;
    if (a>5) {
        return 0;
    }
    printf("%d\n",a);
    add();
    return 0;
}